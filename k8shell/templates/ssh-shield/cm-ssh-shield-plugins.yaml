{{- if and .Values.sshShield.enabled .Values.sshShield.plugins }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: ssh-shield-plugin-configs
  labels:
    {{- include "k8shell.labels" . | nindent 4 }}
    app.kubernetes.io/component: ssh-shield
data:
  {{- range $type, $config := .Values.sshShield.plugins }}
  {{- if eq $type "nfgate" }}
  nfgate.yaml: |
    {{- toYaml (omit $config "authKey") | nindent 4 }}
    authKey: !file ${SECRET_FILES}/nfgate_authkey
  {{- else }}
  {{ $type }}.yaml: |
    {{- toYaml $config | nindent 4 }}
  {{- end }}
  {{- end }}
{{- end }}
