apiVersion: v1
kind: ConfigMap
metadata:
  name: blueprint-base-files
data:
{{- $files := .Files.Glob "files/blueprints/*" }}
{{- range $path, $file := $files }}
  {{ base $path }}: |
{{ $file | toString | indent 4 }}
{{- end }}
