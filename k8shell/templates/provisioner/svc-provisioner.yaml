apiVersion: v1
kind: Service
metadata:
  name: provisioner
  labels:
    {{- include "k8shell.labels" . | nindent 4 }}
    app.kubernetes.io/component: provisioner
spec:
  type: ClusterIP
  {{- if and .Values.grpc .Values.grpc.roundRobin }}
  clusterIP: None
  {{- end }}
  ports:
    - port: 9030
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    k8shell.io/app: provisioner
